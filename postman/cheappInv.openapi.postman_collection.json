{
  "info": {
    "name": "cheappInv (OpenAPI)",
    "_postman_id": "0a6f9d4a-0b0d-4b1e-a3c0-000000000001",
    "description": "Colección base para probar los endpoints del microservicio cheappInv.\n\nNotas:\n- Por defecto la app corre en http://localhost:9090\n- Para trazabilidad usa el header X-Correlation-Id\n- Si corres con perfil dev (default) usa H2 in-memory: los datos no se mantienen al reiniciar\n- Para persistir en Postgres corre con SPRING_PROFILES_ACTIVE=postgres",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Events (in) - PedidoProveedorRecibido (restock)",
      "request": {
        "method": "POST",
        "header": [
          { "key": "Content-Type", "value": "application/json" },
          { "key": "X-Correlation-Id", "value": "{{$guid}}" }
        ],
        "url": {
          "raw": "{{baseUrl}}/api/events/in/pedido-proveedor-recibido",
          "host": ["{{baseUrl}}"],
          "path": ["api", "events", "in", "pedido-proveedor-recibido"]
        },
        "body": {
          "mode": "raw",
          "raw": "{\n  \"eventId\": \"{{$guid}}\",\n  \"correlationId\": \"{{$guid}}\",\n  \"sku\": \"SKU-1\",\n  \"quantity\": 10,\n  \"warehouseId\": \"MAIN\"\n}"
        }
      }
    },
    {
      "name": "Events (in) - ItemAgregado (discount)",
      "request": {
        "method": "POST",
        "header": [
          { "key": "Content-Type", "value": "application/json" },
          { "key": "X-Correlation-Id", "value": "{{$guid}}" }
        ],
        "url": {
          "raw": "{{baseUrl}}/api/events/in/item-agregado",
          "host": ["{{baseUrl}}"],
          "path": ["api", "events", "in", "item-agregado"]
        },
        "body": {
          "mode": "raw",
          "raw": "{\n  \"eventId\": \"{{$guid}}\",\n  \"correlationId\": \"{{$guid}}\",\n  \"sku\": \"SKU-1\",\n  \"quantity\": 1,\n  \"warehouseId\": \"MAIN\"\n}"
        }
      }
    },
    {
      "name": "Inventory - GET Product by SKU",
      "request": {
        "method": "GET",
        "header": [
          { "key": "X-Correlation-Id", "value": "{{$guid}}" }
        ],
        "url": {
          "raw": "{{baseUrl}}/api/products/SKU-1",
          "host": ["{{baseUrl}}"],
          "path": ["api", "products", "SKU-1"]
        }
      }
    },
    {
      "name": "Inventory - GET Stock",
      "request": {
        "method": "GET",
        "header": [
          { "key": "X-Correlation-Id", "value": "{{$guid}}" }
        ],
        "url": {
          "raw": "{{baseUrl}}/api/stock?sku=SKU-1&warehouseId=MAIN",
          "host": ["{{baseUrl}}"],
          "path": ["api", "stock"],
          "query": [
            { "key": "sku", "value": "SKU-1" },
            { "key": "warehouseId", "value": "MAIN" }
          ]
        }
      }
    },
    {
      "name": "Inventory - POST Block product",
      "request": {
        "method": "POST",
        "header": [
          { "key": "X-Correlation-Id", "value": "{{$guid}}" }
        ],
        "url": {
          "raw": "{{baseUrl}}/api/products/SKU-1/block?reason=Producto%20retirado",
          "host": ["{{baseUrl}}"],
          "path": ["api", "products", "SKU-1", "block"],
          "query": [
            { "key": "reason", "value": "Producto retirado" }
          ]
        }
      }
    },
    {
      "name": "Inventory - POST Reactivate product",
      "request": {
        "method": "POST",
        "header": [
          { "key": "X-Correlation-Id", "value": "{{$guid}}" }
        ],
        "url": {
          "raw": "{{baseUrl}}/api/products/SKU-1/reactivate?reason=Vuelve%20disponible",
          "host": ["{{baseUrl}}"],
          "path": ["api", "products", "SKU-1", "reactivate"],
          "query": [
            { "key": "reason", "value": "Vuelve disponible" }
          ]
        }
      }
    }
  ],
  "variable": [
    { "key": "baseUrl", "value": "http://localhost:9090" }
  ]
}
